% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theme_shubham.R
\name{theme_shubham}
\alias{theme_shubham}
\title{Create a Custom ggplot2 Theme}
\usage{
theme_shubham(
  base_size = 11,
  base_family = "Roboto",
  title_family = "Roboto Slab",
  grid = "xy",
  base_line_size = base_size/22,
  base_rect_size = base_size/22
)
}
\arguments{
\item{base_size}{Base font size in points (default: 9)}

\item{base_family}{Base font family for regular text (default: "Roboto")}

\item{title_family}{Font family for titles (default: "Roboto Slab")}

\item{grid}{Controls the grid lines. One of "xy" (both grids), "x" (only x grid),
"y" (only y grid), or "none" (no grid) (default: "xy")}

\item{base_line_size}{Base size for line elements (default: base_size/22)}

\item{base_rect_size}{Base size for rectangular elements (default: base_size/22)}
}
\value{
A ggplot2 theme object
}
\description{
Creates a customized ggplot2 theme based on theme_minimal with enhanced typography
and customized grid lines. This theme uses the Roboto family for base text and
Roboto Slab for titles by default.
}
\details{
The theme checks for font availability and will fall back to system defaults if
required fonts are not installed. It will provide instructions for installing
missing fonts from Google Fonts.
}
\examples{
\dontrun{
library(ggplot2)

# Basic usage with defaults
ggplot(mtcars, aes(wt, mpg)) +
  geom_point() +
  theme_shubham_2()

# Modify grid lines to show only vertical grid
ggplot(mtcars, aes(wt, mpg)) +
  geom_point() +
  theme_shubham_2(grid = "y")
}

}
